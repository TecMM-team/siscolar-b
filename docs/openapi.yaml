openapi: 3.0.3
info:
  title: Sisesco-B API
  description: API técnica para la gestión escolar y validación de repositorios documentales.
  version: 1.0.0
servers:
  - url: http://localhost:3050/api/v1
    description: Servidor de Desarrollo

paths:
  /:
    get:
      summary: Verificar estado de la API
      description: Retorna un mensaje confirmando que la API v1 está funcionando y el entorno actual.
      tags:
        - General
      responses:
        '200':
          description: API funcionando correctamente
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /items:
    get:
      summary: Listar ítems de ejemplo (Paginado)
      description: Endpoint de ejemplo que demuestra la paginación estándar del proyecto.
      tags:
        - Ejemplos
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Lista de ítems paginada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedResponse'

  /repositorios/{id}/validar-conexion:
    post:
      summary: Validar conexión técnica con Google Drive
      description: Verifica que el sistema pueda autenticarse y tenga permisos sobre la carpeta raíz asociada al repositorio.
      tags:
        - Repositorios
      parameters:
        - name: id
          in: path
          description: ID del repositorio en la base de datos
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Conexión validada exitosamente
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidacionResponse'
        '403':
          description: Acceso denegado o configuración incorrecta
        '404':
          description: Repositorio no encontrado

  /validacion-documental/validar:
    post:
      summary: Validar un documento PDF
      description: Ejecuta el flujo de extracción y reglas de validación según el tipo documental.
      tags:
        - Validación Documental
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                archivo:
                  type: string
                  format: binary
                claveDocumental:
                  type: string
                  example: "ACTA_NAC"
      responses:
        '200':
          description: Resultado de la validación
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

components:
  schemas:
    SuccessResponse:
      type: object
      properties:
        data:
          type: object
        meta:
          type: object

    PaginatedResponse:
      type: object
      properties:
        data:
          type: array
          items:
            type: object
        meta:
          type: object
          properties:
            page:
              type: integer
            pageSize:
              type: integer
            total:
              type: integer
            totalPages:
              type: integer
    
    ValidacionResponse:
      type: object
      properties:
        data:
          type: object
          properties:
            repositorioId:
              type: integer
            driveId:
              type: string
            carpetaRaizId:
              type: string
            acceso:
              type: string
              example: "Ok"
            permisos:
              type: object
              properties:
                puedeListar:
                  type: boolean
                puedeCrearCarpetas:
                  type: boolean
        meta:
          type: object
